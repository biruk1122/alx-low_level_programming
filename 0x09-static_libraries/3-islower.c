#include "main.h"

/**
 * _islower - Checks for lowercase character of the Function
 * @c: The character to check
 * Return: 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
