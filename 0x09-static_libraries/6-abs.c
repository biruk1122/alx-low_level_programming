#include "main.h"

/**
 * _abs - Cmputes the absolute value of an integer constant
 * @n: the int to check the intger value
 * Return: the absolute value of intger value
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	} 
	return (-n);
}
